<div class="container">
  <div class="row">
    <div class="col-2"></div>
    <div class="col-4"><%= image_tag @item.get_image(300,300) %></div>
    <div class="col-6">
      <div class="font-weight-bold mb-2 h1"><%= @item.name %></div>
      <div class="h6"><%= @item.introduction %></div>
      <div class="h3 mt-5"> ¥<%= (@item.price * 1.1).to_i %><small>(税込)</small> </div>
    </div>
  </div>
</div>