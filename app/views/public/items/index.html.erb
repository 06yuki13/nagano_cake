<h1>商品一覧</h1>(全件)


<table>
  <tr>
    <% @items.each do |item| %>
    <td>
      <%= image_tag item.get_image(100,100) %><br>
      <%= item.name %><br>
      ¥<%= (item.price * 1.1).to_i %>
    </td>
    <% end %>
  </tr>
</table>

